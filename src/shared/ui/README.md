# shared/ui — 디자인 시스템 자리 (지금 비어 있음)

새 디자인 시스템 확정 시 컴포넌트(Button/Input/Card …)가 여기, 토큰이 `app/globals.css`에 들어온다 (설계 §8, 결정 S1).

그 전까지 각 슬라이스 ui는 Tailwind 기본값으로 임시 스타일링한다 — 도입 시점에 이 폴더의 컴포넌트로 치환하는 것이 국소 변경이 되도록, 화면 쪽에 스타일 로직을 쌓지 말 것.
