# widgets — 다도메인 조합 블록 (지연 생성)

여러 도메인을 조합하는 UI 블록(예: 세미나 상세 = 세미나 + 참석자 명단). entities 간 직접 참조가 금지되므로, 도메인 조합은 이 레이어(또는 views)에서 entities 공개 API를 모아 이룬다.

참고 구조 §3.2의 **지연 생성** 원칙: 첫 필요 시점까지 슬라이스를 만들지 않는다 — 레이어 이름·의존 규칙·이 자리만 예약. 앱 셸(`app/(main)/layout.tsx`)도 다도메인화되면 `widgets/app-shell`로 승격한다.
