hover 툴팁 — 딥 잉크 캡슐, 4px 슬라이드 페이드.

```jsx
const { Tooltip, IconButton } = window.DesignSystem_179b2a;
<Tooltip content="캘린더에 추가"><IconButton variant="outline" label="캘린더">…</IconButton></Tooltip>
```
