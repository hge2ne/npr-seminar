필터 칩 — 지역/전형/계열 등 토글 필터. 선택 시 딥 잉크 채움 + 스프링 팝.

```jsx
const { Tag } = window.DesignSystem_179b2a;
<Tag selected onClick={...}>수시</Tag>
<Tag onClick={...} count={24}>서울</Tag>
<Tag onRemove={...}>학생부종합</Tag>
```
