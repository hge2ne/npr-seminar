표면 컨테이너. elevated(기본)·outline·sunken·brand(딥 잉크)·accent(바이올렛 틴트).

```jsx
const { Card } = window.DesignSystem_179b2a;
<Card interactive onClick={...}>…</Card>
<Card variant="brand" padding="32px">…</Card>
```

- `interactive`면 hover에서 translateY(-4px) 리프트.
